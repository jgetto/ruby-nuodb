language: ruby
rvm:
  - 1.9.3
env:
  - NUODB_ROOT=/opt/nuodb
notifications:
  recipients:
    - buck.robert.j@gmail.com
    - rbuck@nuodb.com
script:
  - NUODB_ROOT=/opt/nuodb bundle exec rake nuodb:install
  - NUODB_ROOT=/opt/nuodb bundle exec rake test install
  - NUODB_ROOT=/opt/nuodb bundle exec rake nuodb:remove
